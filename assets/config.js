/* ============================================================
   config.js  —  여기만 고치면 사이트 설정이 바뀝니다.
   ============================================================ */

window.CONFIG = {
  /* 방 이름 / 봇 이름 */
  ROOM_NAME: "호랑봇",
  BOT_NAME: "호랑봇",

  /* 관리자 키.
     관리자 링크 = index.html?key=여기적은값
     ※ 이건 브라우저에서 확인 가능한 "간이 잠금"입니다.
        진짜 보안이 필요하면 README의 [보안] 항목을 읽어주세요. */
  ADMIN_KEY: "horang2026",

  /* ============================================================
     구글 시트 연동
     여기를 채우면 시트에 적은 내용이 사이트에 그대로 나옵니다.
     사이트에서 직접 수정하는 버튼은 사라지고, 시트가 원본이 됩니다.
     ============================================================ */

  /* 스프레드시트 ID
     시트 주소가 https://docs.google.com/spreadsheets/d/AAA111BBB/edit 라면
     가운데 AAA111BBB 부분입니다. */
  SHEET_ID: "",

  /* 각 화면이 읽어올 탭(시트) 이름 */
  SHEETS: {
    commands: "명령어",
    members: "멤버",
    status: "현황",
    patchnotes: "패치노트"
  },

  /* 시트를 안 쓰고 사이트에서 직접 관리하려면 SHEET_ID 를 비워두세요. */
};
