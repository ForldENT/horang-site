/* ============================================================
   config.js  —  여기만 고치면 사이트 설정이 바뀝니다.
   ============================================================ */

window.CONFIG = {
  /* 방 이름 / 봇 이름 */
  ROOM_NAME: "춘구방",
  BOT_NAME: "춘구봇",

  /* 관리자 키.
     관리자 링크 = index.html?key=여기적은값
     ※ 이건 브라우저에서 확인 가능한 "간이 잠금"입니다.
        진짜 보안이 필요하면 README의 [보안] 항목을 읽어주세요. */
  ADMIN_KEY: "chungu-2026",

  /* 구글 시트 연동 (선택)
     시트 → 파일 → 공유 → 웹에 게시 → CSV 선택 → 나온 주소를 붙여넣기.
     비워두면 사이트에 저장된 내용만 사용합니다. */
  SHEETS: {
    // 열: 명령어, 설명, 분류, 관리자전용(Y/N)
    commands: "",
    // 열: 닉네임, 나이, 키, 전공, 쉬는요일, 취미, MBTI, 매력, 이상형, 흡연주량, 하고싶은말
    members: "",
    // 열: 닉네임, 상태(매칭/외출), 상대, 복귀예정, 메모
    status: ""
  },

  /* 원본 시트 바로가기 버튼에 쓸 주소 (편집용 링크) */
  SHEET_LINKS: {
    commands: "",
    members: "",
    status: ""
  }
};
